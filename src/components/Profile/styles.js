import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  profile: {
    width: 30,
    height: 30,
    borderWidth: 1,
    borderColor: '#fff',
    borderRadius: 50,
  }
});

export default styles;
